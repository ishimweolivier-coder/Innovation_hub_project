# Copy to .env (do not commit .env)
# Frontend env vars — Vite exposes only VITE_* variables

# Backend API URL (omit for dev — Vite proxies /api to localhost:8080)
# VITE_API_URL=https://your-app.onrender.com/api
